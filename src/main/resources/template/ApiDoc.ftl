### 接口描述
${apiDesc}

### URI
此接口${(needToken)?string('需要','不需要')}`token`

```javascript
${apiMethodType} ${apiUrl}
```

### RequestBody
```javascript
${apiRequestParam}
```

### ResponseBody
```javascript
${apiResponseParam}
```

### Quote
- [](Struct-)

${apiMarkdownHead}